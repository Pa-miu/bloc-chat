var React = require('React');

var ChatController = React.createClass({
    render : function() {
        return (
            <ChatController
            />
        );
    }
});

module.exports = ChatController;